<% if @restaurant.errors.any? %>
  <section id="errors" >
    <h2> <%= pluralize(@restaurant.errors.count, "error") %> prevented this restaurant from being saved: </h2>
    <ul>
      <% @restaurant.errors.full_messages.each do |message| %>
        <li><%= message %></li>
        <% end %>
      </ul>
    </section>
    <% end %>


<%= form_for @restaurant, html: {multipart: true} do |form| %>  <%= form.label :name %>
  <%= form.text_field :name %>
  <%= form.submit %>
  <%= form.file_field :image %>

<% end %>
